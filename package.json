{
  "name": "jiyunpark-lib",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "packages": [
    "packages/*"
  ],
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:all": "yarn workspaces foreach -pt run build",
    "publish:all": "yarn workspaces foreach -pt --no-private run publish"
  },
  "author": "jiyunpark",
  "license": "ISC",
  "packageManager": "yarn@3.2.1"
}
